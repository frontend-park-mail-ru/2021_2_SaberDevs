.profile
    .profile__info
      img.profile__photo(src=`${photo}`)
      .profile__description Профилем владеет
        .profile__username #{firstName} #{lastName} (#{login}) 
        .profile__aboutme #{aboutme}
    .cards !{articles}
    #loading &#11088;