include _onAvatarErrorMixin
.profile
  .profile__info
    +avatar.profile__photo(src=`${avatarUrl}`)
    .profile__description
      .profile__username #{firstName} #{lastName} (#{login})
      .profile__aboutme Обо мне:
      | #{description}
  if (!disableArticles)   
    .profile__articles-title Статьи