.sidebar
  .sidebar__nav-links
      a(href="/editor").sidebar__nav-item
          | Добавить  
          br
          | запись
      .sidebar__nav-buttons#sidebarTopBlockContent !{topBlockContent}
  .sidebar__content
      .sidebar__categories
          .sidebar__categories-title.sidebar__item
            a(href='/categories') Категории
          if Array.isArray(categoriesList)
            each category, idx in categoriesList
              if (idx > limit)
                .sidebar__categories-item.sidebar__item
              else
                .sidebar__categories-item.sidebar__item(display: 'none')
                  a(href=`/categories/${category}`) #{category}
      .sidebar-bottom_right
          .contacts
              .contacts__title Contacts
              a.contacts__developer Denactive
              a.contacts__developer MollenAR
              a.contacts__developer Yurij-lyubskij
              a.contacts__developer Svetlanlka
              