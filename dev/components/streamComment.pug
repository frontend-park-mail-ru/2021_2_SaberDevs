.stream-comment(id=`${id}`)
    .stream-comment__author
        img.stream-comment__author-photo(src=`${avatarUrl}`)
        .stream-comment__author-name #{firstName} #{lastName}
    .stream-comment__text #{text}